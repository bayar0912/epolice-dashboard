############################################
# Example environment file (no real secrets)
# Copy to .env and fill values locally or in Vercel.
# Never commit real passwords/connection strings.
############################################

# MySQL (local/dev or managed MySQL)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_password_here
DB_NAME=epolice_db
DB_PORT=3306

# App port (local only)
PORT=4000

# Choose ONE of the following DATABASE_URLs and set it in your real .env

# MySQL example
DATABASE_URL="mysql://root:your_password_here@localhost:3306/epolice_db"

# PostgreSQL (Neon/Supabase) example — replace USER, PASS, HOST and DB
#DATABASE_URL="postgresql://USER:PASS@HOST:5432/epolice_db?sslmode=require"

# JWT secret placeholder
JWT_SECRET=your_jwt_secret_key_here