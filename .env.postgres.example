# Example .env for PostgreSQL providers like Neon or Supabase
# Copy to .env and fill credentials

# Neon example (SSL required)
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/epolice_db?sslmode=require"

JWT_SECRET=your_secure_jwt_secret
PORT=3000
